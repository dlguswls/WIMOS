<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link href="stylesheets/main.css" rel="stylesheet" />
        <link href="stylesheets/write.css" rel="stylesheet" />
        <style>
            .btn {
            display: inline-block;
            font-weight: 400;
            line-height: 1.5;
            color: #212529;
            text-align: center;
            text-decoration: none;
            vertical-align: middle;
            cursor: pointer;
            -webkit-user-select: none;
                -moz-user-select: none;
                -ms-user-select: none;
                    user-select: none;
            background-color: transparent;
            border: 1px solid transparent;
            padding: 0.375rem 0.75rem;
            font-size: 1rem;
            border-radius: 0.25rem;
            transition: color 0.15s ease-in-out, background-color 0.15s ease-in-out, border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
            }
            @media (prefers-reduced-motion: reduce) {
            .btn {
                transition: none;
            }
            }
            .btn:hover {
            color: #212529;
            }
            .btn:focus {
            outline: 0;
            box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
            }
            .btn:disabled, .btn.disabled, fieldset:disabled .btn {
            pointer-events: none;
            opacity: 0.65;
            }
            .btn-outline-dark {
            color: #212529;
            border-color: #212529;
            }
            .fw-bolder {
            font-weight: bolder !important;
            }
            .writetable {
                width:80%; 
                margin-left:auto; 
                margin-right:auto;
                margin-top : 2%;
                border-collapse: collapse;
                line-height: 1.5;
                background-color: #FFFFFF;
            }
            .writetable th {
                width:10%;
                padding: 10px;
                font-weight: bold;
                text-align:center;
                vertical-align: top;
                background-color:#EAEAEA;
            }

            .writetable td {
            padding: 10px;
            font-weight: bold;
            text-align:center;
            vertical-align: top;
            }

            .writetable td input{
                width:100%;
                border: #FFFFFF;
            }
            .writetable td textarea{
                width:100%;
                border: #FFFFFF;
            }    

            .listbtn {
                width:80%;
                float:right;
                margin-left: auto;
                margin-top:10rem;
            }
            .write{
                float:right;
                margin-top:0.5rem;
            }
        </style>
        <script src="https://code.jquery.com/jquery-3.4.1.js"></script>
        <script>
            $(document).ready(function(){
                $('#modifybtn').click(function(){
                    alert('글을 수정하시겠습니까?');
            });});
        </script>
    </head>
    <body>
        <div style="width:80%;margin-top:4%;margin-left:auto;margin-right:auto;">
            <h1 class = 'title fw-bolder'><%= title %></h1>
        </div>
        <form action="/update" method="post">
            <table class="writetable table-bordered" border="1">
                <input type="hidden" name="idx" value="<%=row.idx%>"/>
                <tr>
                    <th>작성자</th>
                    <td><input style="font-family:JejuMyeongjo;font-size:1.2rem;" type="text" name="name" id="name" value="<%=row.name%>" required/></td>
                </tr>
                <tr>
                    <th>제목</th>
                    <td><input style="font-family:JejuMyeongjo;font-size:1.2rem;" type="text" name="title" id="title" value="<%=row.title%>" required/></td>
                </tr>
                <tr>
                    <th>내용</th>
                    <td>
                        <img style="float:left;width:40rem;" src="<%=row.image%>" alt="image"/>
                        <textarea style="font-family:JejuMyeongjo;font-size:1.2rem;" name="content" id="content" cols="30" rows="10" required><%=row.content%></textarea>
                    </td>
                </tr>
                <tr>
                    <th>패스워드</th>
                    <td><input type="password" name="passwd" id="passwd" required/></td>
                </tr>
                <tr>
                    <th>변경일</th>
                    <td><%=row.modidate%></td>
                </tr>
                <tr>
                    <th>등록일</th>
                    <td><%=row.regdate%></td>
                </tr>
                <tr>
                    <th>조회수</th>
                    <td><%=row.hit%></td>
                </tr>
            </table>
            <div style="width:80%;margin-left:auto;margin-right:auto;">
                <button id="modifybtn" style="margin-left:1%" type="submit" class="btn btn-outline-dark fw-bolder write">수정</button>
                <a href="/community" class="btn btn-outline-dark fw-bolder write">목록</a>
            </div>
        </form>
 
    </body>
</html>